<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.learning.mapper.StudyPlanMapper">

    <select id="findByUserIdOrderByCreatedAtDesc" resultType="com.campus.learning.entity.StudyPlan">
        SELECT * FROM study_plan
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="findReminders" resultType="com.campus.learning.entity.StudyPlan">
        SELECT * FROM study_plan
        WHERE user_id = #{userId}
          AND status = 'ACTIVE'
          AND remind_date &lt;= CURDATE()
          AND reminder_sent = 0
        ORDER BY remind_date ASC
    </select>

    <update id="markReminderRead">
        UPDATE study_plan
        SET reminder_sent = 1
        WHERE id = #{id}
    </update>

</mapper>
