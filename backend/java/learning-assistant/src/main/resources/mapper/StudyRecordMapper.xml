<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.learning.mapper.StudyRecordMapper">

    <select id="findByUserIdAndStudyDateBetween" resultType="com.campus.learning.entity.StudyRecord">
        SELECT * FROM study_record
        WHERE user_id = #{userId}
          AND study_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY study_date ASC
    </select>

    <select id="findByTaskId" resultType="com.campus.learning.entity.StudyRecord">
        SELECT * FROM study_record
        WHERE task_id = #{taskId}
        ORDER BY created_at DESC
    </select>

</mapper>
